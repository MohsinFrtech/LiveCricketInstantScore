<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/splashicon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:name=".MyApp"
        android:roundIcon="@drawable/splashicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiveCricketInstantScore"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.FirstScreen"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/FirstScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="" />
        <activity
            android:name=".ui.activities.StreamingScreen"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:theme="@style/PlayerTheme">

        </activity>
        <activity
            android:name=".ui.activities.SecondActivity"
            android:theme="@style/ClassTheme"
            android:exported="true">

        </activity>
    </application>

</manifest>